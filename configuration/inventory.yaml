all:
  children:
    main:
      hosts:
        10.1.1.69:
    hosts:
      worker:
        10.1.1.10:
  vars:
    ansible_user: jenkins
    ansible_ssh_private_key_file: "~/.ssh/training.pem"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"